"""
Publishers — Platform-specific publishing connectors.

Each publisher implements a common interface for publishing articles
to a specific platform. The Publish Agent discovers available
publishers via the ToolRegistry.
"""
